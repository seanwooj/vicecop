.section-header
  %h3 {{ vice.name }}

%button.add-vice.btn.btn-primary{'ng-hide' => 'userVice.active', 'ng-click' => 'addUserVice()'}
  Start Tracking

%button.add-vice.btn.btn-primary{'ng-show' => 'userVice.active', 'ng-click' => 'deactivateUserVice()'}
  Stop Tracking

.tracking
  {{ vice.user_vices.length }} tracking